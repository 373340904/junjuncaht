# JunjunChat 部署指南

## 架构说明

JunjunChat 分为两部分：
- **前端**：静态网页（HTML/CSS/JS），部署到 Netlify
- **后端**：Python FastAPI 服务，需要部署到支持 Python 的平台（Render / Railway / 本地服务器）

> ⚠️ Netlify 只能托管静态前端，不能运行 Python 后端。后端必须单独部署。

---

## 一、后端部署（任选一种）

### 方案A：本地运行（推荐，免费）

1. 安装 Python 3.10+
2. 安装依赖：
```bash
cd server
pip install -r requirements.txt
```
3. 启动：
```bash
python main.py
```
4. 后端运行在 `http://localhost:8000`

### 方案B：Render 部署（需信用卡验证）

1. 将项目推送到 GitHub
2. 在 Render 创建 Web Service
3. 配置：
   - Build Command: `pip install -r requirements.txt`
   - Start Command: `uvicorn main:app --host 0.0.0.0 --port $PORT`
   - 环境变量：设置 `DATABASE_URL` 为 PostgreSQL 连接串

### 方案C：Railway / Fly.io 等

类似 Render，支持 Python 即可。

---

## 二、前端部署到 Netlify

### 步骤1：修改 API 地址

部署前，需要把前端和拓展里的 API 地址改成你的后端地址：

**文件1：`web/js/app.js`**
```javascript
const CONFIG = {
  API_BASE: 'https://你的后端地址/api/v1',
  WS_BASE: 'wss://你的后端地址'
};
```

**文件2：`junjunchat-extension.js`**
```javascript
const API_BASE = 'https://你的后端地址/api/v1';
const WS_BASE = 'wss://你的后端地址';
```

### 步骤2：部署到 Netlify

**方式一：拖拽部署（最简单）**

1. 打开 https://app.netlify.com/drop
2. 把 `web` 文件夹拖进去
3. 完成！获得一个 `https://xxx.netlify.app` 地址

**方式二：GitHub 自动部署**

1. 将项目推送到 GitHub
2. 在 Netlify 点 "Add new site" → "Import an existing project"
3. 选择你的仓库
4. 配置：
   - Build command: 留空
   - Publish directory: `web`
5. 部署

---

## 三、本地开发测试

1. 启动后端：`cd server && python main.py`
2. 浏览器打开 `web/index.html`
3. 注册账号，自动加入官方群
4. 开始聊天！

---

## 四、管理员功能

- 管理员密码：`225878`（可在后端 `ADMIN_PASSWORD` 环境变量修改）
- 入口：网页端 → 设置 → 输入管理员密码 → 进入管理后台
- 功能：查看统计数据、用户列表、删除用户

---

## 五、机器人开发

1. 网页端 → 机器人 → 创建机器人
2. 复制 Bot Key
3. 参考 `BOT_API.md` 开发机器人
4. 支持 Python 和 JavaScript

---

## 六、环境变量

后端支持以下环境变量：

| 变量 | 说明 | 默认值 |
|------|------|--------|
| `JWT_SECRET_KEY` | JWT 密钥 | 随机生成 |
| `DATABASE_URL` | 数据库连接 | SQLite 本地 |
| `ADMIN_PASSWORD` | 管理员密码 | 225878 |
| `ACCESS_TOKEN_EXPIRE_MINUTES` | Token 有效期 | 10080 (7天) |

---

## 七、项目结构

```
junjunchat-full/
├── web/                    # 前端（部署到 Netlify）
│   ├── index.html
│   ├── css/style.css
│   ├── js/app.js
│   ├── logo.png
│   └── netlify.toml
├── server/                 # 后端（Python）
│   ├── main.py
│   ├── requirements.txt
│   ├── Dockerfile
│   └── render.yaml
├── junjunchat-extension.js # CCW 拓展
├── BOT_API.md              # 机器人 API 文档
├── 部署指南.md              # 本文件
└── start.bat               # 本地启动脚本
```
